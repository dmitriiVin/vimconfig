" ================================
" ===      ОСНОВНОЙ ФАЙЛ       ===
" ================================

" === Подключаем автонастройки ===
source ~/.vim/vimconfigs/autocmd.vim

" === Подключаем функции ===
source ~/.vim/vimconfigs/functions.vim

" === Подключаем бинды ===
source ~/.vim/vimconfigs/mappings.vim

" === подключаем опции ===
source ~/.vim/vimconfigs/options.vim

" === Подключаем плагины ===
source ~/.vim/vimconfigs/plugins.vim

